version: '3.8'

services:
  belgaauth:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5000
    volumes:
      - ./auth.db:/app/auth.db  # Persistir banco de dados
    restart: unless-stopped






